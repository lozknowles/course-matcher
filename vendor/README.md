# Generated runtime dependencies

Run `npm install && npm run vendor` to populate this directory with pinned local browser assets for Tesseract.js OCR, English trained data and PDF.js.

The generated third-party files are intentionally not hand-edited. Their respective upstream licences apply.
